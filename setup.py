from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='In this project, I will be building a credit fraud detector that will (ideally) outperform the previous predictor used to make predictions on this dataset.',
    author='Kishen Sharma',
    license='MIT',
)
